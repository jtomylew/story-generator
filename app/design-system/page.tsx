'use client'

// import { GenerateStoryScreen } from '@/components/screens/GenerateStoryScreen'
// import { Button, Card, CardContent, CardDescription, CardHeader, CardTitle, Badge, Spinner, Skeleton } from '@/components/ui'

export default function DesignSystemPage() {
  return (
    <div className="min-h-screen bg-background p-8">
      <div className="max-w-6xl mx-auto space-y-12">
        <h1 className="text-4xl font-bold text-center">Design System - Under Construction</h1>
        <p className="text-center text-muted-foreground">This page is being updated after subframe sync.</p>
      </div>
    </div>
  );
}